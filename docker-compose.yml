version: "3.9"
services:
  api:
    restart: "always"
    build:
      context: "./"
      dockerfile: "Dockerfile"
      args:
        IMAGE_REGISTRY_BASE_URL: ${IMAGE_REGISTRY_BASE_URL}
    ports:
      - "${API_PORT}:${API_PORT}"
    env_file:
      - .env
